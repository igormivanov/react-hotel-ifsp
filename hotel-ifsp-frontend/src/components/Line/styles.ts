import styled from "styled-components";

export const LineContainer = styled.div`
  width: 300px;
  border-bottom: 2px solid ${({theme}) => theme.colors["brand-gold-100"]};
  padding-bottom: 1rem;
  margin-bottom: 3rem;
`